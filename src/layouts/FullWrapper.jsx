// src/layouts/FullWrapper.jsx
import React from 'react';

const FullWrapper = ({ children }) => {
  return (
    <div className="full-wrapper">
      {children}
    </div>
  );
};

export default FullWrapper;
